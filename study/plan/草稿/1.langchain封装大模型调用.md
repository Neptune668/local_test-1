## 1.langchain封装大模型调用

```python
# 此脚本中展示一个更加方便的调用方式：ChatOpenAI()
from dotenv import load_dotenv
import os

from langchain_openai import ChatOpenAI

# load_dotenv()
# llm = ChatOpenAI(
#     model ="gpt-4o-mini",
#     base_url = os.getenv("OPENAI_BASE_URL"),
#     api_key = os.getenv("OPENAI_API_KEY")
# )
load_dotenv()
llm = ChatOpenAI(model = "gpt-4o-mini")
response = llm.invoke("介绍一下尚硅谷")
print(response.content)
```

## 2.langchain封装大模型格式化输出

```python
# 此脚本用于尝试使用LangChain封装模型厂商提供的输出解析功能
# 需要在调用的时候，将输出的数据类型作为参数一起上传过去
from dotenv import load_dotenv
from langchain_openai import ChatOpenAI
from pydantic import BaseModel

load_dotenv()

llm = ChatOpenAI(model ="gpt-4o-mini")

# 定义返回的数据类型
class CalendarEvent(BaseModel):
    name: str
    date: str
    participants: list[str]
# 将返回类型的这个类，注册到大模型对象中
new_llm = llm.with_structured_output(schema = CalendarEvent)

response = new_llm.invoke("Alice and Bob are going to a science fair on Friday.")
print(response)
print(type(response))
```

## 3.在进行模型调用的时候，需要构建合适的消息列表结构

```python
# 在进行模型调用的时候，需要构建合适的消息列表结构
# 四种格式：
# 1.字符串
# 2.tuple
# 3.dict
# 4.消息对象
from dotenv import load_dotenv
from langchain_core.messages import SystemMessage, HumanMessage, AIMessage
from langchain_openai import ChatOpenAI

load_dotenv()
llm = ChatOpenAI(model = 'gpt-5.5')
# 构造待传入的消息
input_string ="评价一下尚硅谷"
# string_response = llm.invoke(input_string)
# print(string_response.content)
input_tuple =[
    ("system","你是一个美国人，只会说英文"),
    ("user","你好，今天过得怎么样ovo")
]
# tuple_response = llm.invoke(input_tuple)
# print(tuple_response.content)
input_dict = [
    {"role":"system","content":"你是一个韩国人，只会说韩语"},
    {"role":"user","content":"哈喽"}
]
# dict_response = llm.invoke(input_dict)
# print(dict_response.content)
input_message_object = [
    SystemMessage(content="你是一个小狗，只会汪汪叫，被踹了会逃跑"),
    HumanMessage(content="（吹了个口哨）"),
    AIMessage(content="汪汪！"),
    HumanMessage(content="哪来的狗？（踹了你一脚）")
]
message_object_response = llm.invoke(input_message_object)
print(message_object_response.content)
```

## 4.流式调用

#### 4.1

```python
# 此脚本用于展示LangChain中的流式调用
# 使结果实现打字机效果的返回
from dotenv import load_dotenv
from langchain_openai import ChatOpenAI

load_dotenv()
llm = ChatOpenAI(model="gpt-5.5")

response = llm.stream(
    "写一个1000字的短片小说，要求感人且荒诞中带有意思人性的光辉"
)
for chunk in response:
    print(chunk.content,end="")
#变身成边输出
```

#### 4.2异步调用

```python
# 这个例子展示异步调用大模型服务
# IO密集型，异步的方式更好地能够释放CPU资源
import asyncio

from dotenv import load_dotenv
from langchain_openai import ChatOpenAI

load_dotenv()

async def async_invoke():
    llm = ChatOpenAI(model = "gpt-4o-mini")
    response = await llm.ainvoke("你好")
    print(response.content)

if __name__ == "__main__":
    asyncio.run(async_invoke())
```

## 5.提示词模板

```python
# 此脚本用于展示提示词模板的功能，构建出一个提示词模板对象，通过invoke进行参数的传入，搭建一个完整的提示词
# 将提示词放入大模型中进行调用
from dotenv import load_dotenv
from langchain_core.prompts import ChatPromptTemplate
from langchain_openai import ChatOpenAI

load_dotenv()
chat_prompt_template = ChatPromptTemplate.from_messages(
    messages=[
        ("system","你是一个专业的评论员，善于使用刻薄刁钻且具有深度的分析去处理问题"),
        ("user","评价一下{product}的优缺点，包含两个方面：{aspect1}，和{aspect2}")
    ]
)
prompt = chat_prompt_template.invoke(
    {"product":"IPhone手机","aspect1":"价格","aspect2":"外观"}
)
print(prompt)
print("===================================================================================")
llm = ChatOpenAI(model = "gpt-5.5")
response = llm.invoke(prompt)
print(response.content)
```

## 6.Chains(重点)

#### 6.1链式调用

```python
# 此脚本演示使用LangChain中的链式处理
# 搭建了一个Runnable的链，链里面有几个组件，首尾相连。
# 前者的输出，作为后者的一个输入
from dotenv import load_dotenv
from langchain_core.output_parsers import StrOutputParser
from langchain_core.prompts import PromptTemplate
from langchain_openai import ChatOpenAI

load_dotenv()
# 构建第一个组件 -> 提示词模板
prompt_template = PromptTemplate(
    template="讲一个关于{topic}的冷笑话",
    input_variables=["topic"]
)
# 构建第二个组件 -> 大模型对象
llm = ChatOpenAI(model = "gpt-5.5")

# 构建第三个组件 -> 输出解析器
parser = StrOutputParser()

# 构建链，实现三个组件的连接操作
chain = prompt_template | llm | parser

response = chain.invoke({"topic":"鼠标"})

print(response)

```

#### 6.2并行

```python
# 此脚本用于展示链的并行运行（同时运行多条链）
from dotenv import load_dotenv
from langchain_core.output_parsers import StrOutputParser
from langchain_core.prompts import PromptTemplate
from langchain_core.runnables import RunnableParallel
from langchain_openai import ChatOpenAI

load_dotenv()

# 英文翻译链的提示词模板组件
english_prompt_template = PromptTemplate.from_template("把这句话翻译成英文：{topic}")
# 韩文翻译链的提示词模板组件
korean_prompt_template = PromptTemplate.from_template("把下面这句话翻译成韩文：{topic}")

# 定义大模型对象的组件
llm = ChatOpenAI(model = "gpt-4o-mini")
# 定义字符串的解析器
parser =  StrOutputParser()

# 定义两条链
english_chain = english_prompt_template | llm | parser
korean_chain = korean_prompt_template | llm | parser

map_chain = RunnableParallel(
    english = english_chain,
    korean = korean_chain
)

response = map_chain.invoke({"topic":"我爱尚硅谷"})
print(response)
```

提升

```python
# 此脚本用于展示复杂的链的调用过程，具体怎么做，如图所示：
# 两个链负责单纯地被调用，结果送入到第三个链中。以上这个过程全部被封装到了第四个链中
from dotenv import load_dotenv
from langchain_core.output_parsers import StrOutputParser
from langchain_core.prompts import PromptTemplate
from langchain_openai import ChatOpenAI

load_dotenv()

# 定义出大模型对象，作为组件添加到链中
llm = ChatOpenAI(model = "gpt-5.5")
# 定义出解析器对象，作为组件添加到链中
parser = StrOutputParser()

prompt_template_1 = PromptTemplate.from_template("正面评价一下{topic}这个问题")
prompt_template_2 = PromptTemplate.from_template("负面评价一下{topic}这个问题")

prompt_template_sum = PromptTemplate.from_template("根据这两个分析：{view_1},和{view_2},给出一个汇总的结果")

chain_1 = prompt_template_1 | llm | parser
chain_2 = prompt_template_2 | llm | parser
chain_sum = prompt_template_sum | llm |parser
# 构建第四个链 -> 映射链
map_chain = {
    "view_1":chain_1,
    "view_2":chain_2
} | chain_sum

response = map_chain.invoke({"topic":"加班"})
print(response)
```